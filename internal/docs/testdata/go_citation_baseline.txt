# Research citations in Go comments that did not resolve when
# TestGoCommentCitationsResolve was introduced. One bracket body per line,
# exactly as it appears between the brackets. The test fails on a citation
# that is dangling and NOT listed here; it logs entries here that no longer
# dangle so they can be removed. Fix the comment (or restore the heading the
# comment cites) and delete the line — this file only shrinks.
#
# Empty: every research citation in a Go comment resolves (CL-6).
