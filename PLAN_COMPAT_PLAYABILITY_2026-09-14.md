# Download compatibility and playability follow-up

Authorized scope: CV-04, CV-11/12, and focused investigation of GP-03 and
HI-06/11. Starting main: `58397146`. This is task state, not retail evidence.

## Ownership and public contracts

- Root: content read diagnostics and required-unit preflight, content design,
  this plan, integration, independent review and landing.
- GAF unit: GAF metadata/pixel readers and their focused fixtures; owning GAF
  format and category 02 malformed-GAF research. Preserve public loader APIs,
  file bounds and the existing aggregate expansion safeguards. Root owns
  DESIGN_CONTENT_VFS and integrates the unit's requested wording there.
- Input unit: command-panel right-click routing and captured drag-scroll
  investigation, scoped input/camera files and their tests; interface design
  and category 07 research. Existing command and camera APIs remain; settle
  retail behavior before changing quantization or gesture ownership.
- Transport unit: stock carried-model rendering investigation and any proven
  executor correction; GPU model files and focused tests, GPU/presentation
  design and category 03 research. Investigation also owns client
  `model_staging.go` and a new staging-scale regression fixture for the
  reproduced classic zoom defect. Existing draw-list and simulation APIs remain.

Content diagnostics preserve discovered incomplete unit records and their
identity, but explain missing secondary data and refuse required incomplete
definitions in preflight. Feature read failures must retain their source cause
instead of silently dropping records. No replacement definitions are invented.

## Verification

Baseline fast/retail gates precede independent dispatch. Workers run focused
contracts, then coordinate full gates to avoid redundant simultaneous corpus
runs. Root reads complete diffs and evidence, repeats required checks in the
assigned review trees, integrates current main and runs post-landing checks.
Visual changes require inspected captures; applicable performance runs use
the documented scripts sequentially. External artifacts live under
`/private/tmp/nanolathe-compat-artifacts`. Remove only this task's merged trees.

## Progress

- Baseline fast and retail gates passed on `58397146`.
- CV-04: GAF row compatibility implemented and independently reviewed against
  the private evidence. Corrected the old format description of a clipped
  literal's source advance before implementing it. All 426 winning stock
  animation/texture banks retain identical decoded fingerprints.
- CV-11/12: feature read errors now survive catalog loading; incomplete unit
  records produce deterministic source diagnostics and required preflight
  rejection. The authored download archive fixture passes. Independent review
  caught host filenames in OS error text; the follow-up preserves the original
  error chain while displaying portable resource diagnostics.
- HI-06: the proposed unconditional command-gadget cancellation was disproved
  by the earlier GUI ownership pass. Fixed the actual omission: blank, hidden
  and grey command-window areas now retain ownership of an inside down.
  Factory product right-release subtraction remains covered.
- HI-11: no production change needed. Input is already sampled at 30 Hz;
  extra draws do not step drag scrolling. Signed per-service truncation and
  release handling match the evidence and have a regression fixture.
- Combined GAF, diagnostics and input tree `a6136fb4` passed fast and retail
  gates. Root reviewed the diffs and the GAF/input arithmetic against the
  private sources; the GAF worker independently reviewed diagnostics.
- GP-03: normal stock Atlas/Peewee and Valkyrie/AK pickup/flight/unload
  investigations did not reach the proposed modern child skip. Close-up
  captures instead reproduced a classic zoom defect: native child rasters
  staged using already-scaled attachment offsets, then the final blit scaled
  those offsets again. Fixed by projecting the relative position at native
  raster scale, preserving each child's retained shadow/trace anchor. Tests
  cover positive/negative offsets at 1x, 1.5x and 2x. Root inspected the actual
  corrected captures and independently verified all 18 expected unchanged
  native/modern PNG comparisons.

## Final validation and disposition

All scoped implementation and investigation work is complete. Integrated
current main `65df8d4d` before the final checks; the unit-limit change required
refreshed performance baselines. Root's final integrated `tools/check`,
`tools/check-retail` and real-device `TestDeviceFixtureLoop` passed on
`3cae79f6`. Worker gates also passed; independent diff, citation, arithmetic,
clean-room and invariant reviews found no outstanding correction.

The matching `current-before-*` and `current-after-*` battle artifacts use
Great Divide, seed 7, 1920x1080, 2x scale and 180 measured draws. Within each
renderer, metadata and every per-frame workload census match. Modern's final
PNG is byte-identical; classic changes the corrected attached-product
placement. Both final captures were inspected. Eight builds, moving armies,
sprite features and burning features are present throughout the measured run.

| Host metric | Classic before / after | Modern before / after |
|---|---|---|
| Mean record time | 19.457 / 18.724 ms | 2.795 / 2.941 ms |
| p95 draw work | 27.965 / 26.930 ms | 10.828 / 10.543 ms |
| Median cadence | 33.334 / 33.334 ms | 33.333 / 33.333 ms |

These are scene-specific host timings, not GPU timestamps or a universal frame
rate claim. Classic and modern are compared against their own baselines;
their effect settings differ. The earlier `before-*` artifacts precede the
unit-limit integration and are not used for the final comparison.

The separate nested-cargo concern remains bounded: current stock admission
does not produce the unsupported modern packets. Arbitrary mod/save nested
carriage is unverified, so no speculative fallback was added. The owning GPU
design and external transport report retain this limitation.

Root owns landing, post-landing checks and removal of only this task's merged
worktrees. Post-landing outputs are kept in `postlanding-check.log`,
`postlanding-check-retail.log` and `postlanding-device.log` under the external
artifact directory named above.
